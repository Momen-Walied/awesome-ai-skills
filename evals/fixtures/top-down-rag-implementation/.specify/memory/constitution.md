# Retrieval service constitution

- Preserve tenant isolation before ranking or context assembly.
- Keep the dense route available as rollback.
- Trace every retrieval strategy selected by the service.
- Implement acceptance scenarios in `specs/001-exact-sku-retrieval/spec.md`.
